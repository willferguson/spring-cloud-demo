spring:
  profiles:
    active: dev
  application:
    name: stuffproducer
  cloud:
    config:
      uri: http://localhost:8888
      username: admin
      password: admin
server:
  port: 9091
management:
  endpoints:
    web:
      exposure:
        include: "*"