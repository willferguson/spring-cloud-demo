spring:
  profiles:
    active: dev
  application:
    name: thingproducer
  cloud:
    config:
      uri: http://localhost:8888
      username: admin
      password: admin
server:
  port: 9090
management:
  endpoints:
    web:
      exposure:
        include: "*"